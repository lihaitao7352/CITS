我想做一个功能图像的Excle说明文档
下面是想要在画面上实现的功能java，数据库是oracle
功能1；我想要搜索年份变更、组织变更和预算数据删除。
功能2；我想要更改会计年度、重组组织并删除预算数据。


本番保守、SBX32、SBX40のオラクル環境を整備したい。
一次（準備工程）
　①３つの環境のオブジェクトの一覧を作成し、差異を確認する。
　②本番保守を正とし、SBX32、SBX42に不足分のオブジェクトを作成したいのでクエリの作成をする。
　③バックアップテーブルも削除したいため、削除クエリも作成する。

Oracle環境で本番保守、SBX32、SBX40の整備を行い、一次準備工程としてのタスクを計画する方法を説明します。

### 一次（準備工程）

#### ① ３つの環境のオブジェクトの一覧を作成し、差異を確認
まず、各環境（本番保守、SBX32、SBX40）で使用されているオブジェクトの一覧を作成する必要があります。これには以下のクエリを各環境で実行して、オブジェクトの一覧を取得します。

```sql
SELECT OBJECT_TYPE, OBJECT_NAME
FROM ALL_OBJECTS
ORDER BY OBJECT_TYPE, OBJECT_NAME;
```

このクエリは、それぞれの環境におけるオブジェクトの種類と名前を一覧表示します。結果をExcelなどの比較ツールで比較し、差異を特定します。

#### ② 本番保守を正とし、SBX32、SBX40に不足分のオブジェクトを作成
差異が確認できたら、不足しているオブジェクトを他の環境に作成するためのクエリを作成します。例えば、本番環境にあってSBX32にないテーブルがある場合、以下のようなDDL文（データ定義言語）を使用してテーブルを作成します。

```sql
CREATE TABLE new_table_name (
    column1 datatype,
    column2 datatype,
    column3 datatype,
    ...
);
```

不足しているオブジェクトのタイプに応じて適切なSQL文を作成します（例：テーブル、ビュー、ストアドプロシージャなど）。

#### ③ バックアップテーブルも削除したいため、削除クエリも作成
不要なバックアップテーブルや古いデータを削除するためのクエリを作成します。テーブルを削除する基本的なSQLコマンドは次の通りです。

```sql
DROP TABLE table_name;
```

安全のため、`DROP TABLE`を実行する前に、特定のテーブルが存在することを確認するクエリも実行することをお勧めします。

```sql
SELECT * FROM all_tables WHERE table_name = 'your_table_name';
```

これらのステップに従って、一次準備工程を進めることができます。何か不明な点や追加で必要な情報があれば教えてください。



=NOT(ISERROR(MATCH(A2, Sheet2!A:A, 0)))
